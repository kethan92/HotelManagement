using HotelManagementMVC_ClientWithAngularJS.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Web;

namespace HotelManagementMVC_ClientWithAngularJS.Models
{
    public class UserClient
    {
        /*
       

        public User checkUser(User user)
        {
            try
            {
                HttpClient client = new HttpClient();
                client.BaseAddress = new Uri(Base_URL);
                client.DefaultRequestHeaders.Accept.Add(new 
                    System.Net.Http.Headers.MediaTypeWithQualityHeaderValue("application/json"));

                HttpResponseMessage response = client.GetAsync("User/checkUser?username=" + user.username +
                    "&password="+user.password).Result;
                if (response.IsSuccessStatusCode)
                    //return response.Content.ReadAsAsync<User>().Result;
                    return null;
                return null;
            }
            catch
            {
                return null;
            }
           
        }
        */

      //  public User findUser()
    }
}